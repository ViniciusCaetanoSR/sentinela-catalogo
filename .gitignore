__pycache__/
*.pyc
.venv/
venv/
env/
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
.env
*.zip
site/
# dados/ não vive mais aqui: o estado do bot é a branch órfã "dados", e
# esta pasta é o worktree dela (git worktree add dados dados). Enquanto o
# git a ignorar, o bot nunca mais empurra em main - foi isso que abriu
# caminho para exigir PR e o check "ci" nesta branch. Ver o CONTRIBUTING,
# seção "Mover dados/ para a branch própria".
/dados/
*.tmp
.coverage
.ruff_cache/

# Rascunhos de divulgação: avaliação franca de terceiros que estão sendo
# abordados, num repositório público. Fica no disco, sai do repo.
divulgacao/
