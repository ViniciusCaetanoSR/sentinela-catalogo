__pycache__/
*.pyc
.venv/
venv/
env/
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
.env
*.zip
site/
# Mapa completo NCM -> atributos: ~4,8 MB regenerados todo dia, consumidos
# pelo gerador no mesmo run do CI. Versionar isso seria 4,8 MB de churn
# diário. O render sem coleta (render.yml) o recupera do cache do Actions.
dados/completo.json
# O ZIP oficial do dia, como veio da Receita (já coberto por *.zip, mas
# explícito para ninguém achar que foi esquecido). Os bytes mudam a cada
# pedido; o que vale guardar é o Release bruto-<versao> que o workflow cria.
dados/bruto.zip
*.tmp
.coverage
.ruff_cache/

# Rascunhos de divulgação: avaliação franca de terceiros que estão sendo
# abordados, num repositório público. Fica no disco, sai do repo.
divulgacao/
